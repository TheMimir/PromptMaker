# Core dependencies
streamlit>=1.37.0,<2.0.0
python-dateutil>=2.9.0,<3.0.0

# Security dependencies
bleach>=6.0.0,<7.0.0        # HTML sanitization to prevent XSS
jsonschema>=4.20.0,<5.0.0   # JSON validation

# Timezone support
pytz>=2024.1

# PDF generation (optional)
reportlab>=4.0.0,<5.0.0
